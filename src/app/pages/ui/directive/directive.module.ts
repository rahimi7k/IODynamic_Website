import { NgModule } from '@angular/core';
import { CloseImageDirectrive } from './close-img';

@NgModule({
	declarations: [CloseImageDirectrive],
	exports: [CloseImageDirectrive],
})
export class DirectiveModule { }
